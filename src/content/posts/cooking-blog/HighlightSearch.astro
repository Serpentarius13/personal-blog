---
import Highlighter from "./Highlighter.astro";

interface Props {
  id?: string;
}

const { id } = Astro.props;
---

<Highlighter
  id={id ?? "cooking-search-highligher"}
  targetId="search-trigger"
  activeClassname="ring-2 ring-red-500 scale-[2] bg-base-100 z-[20]"
  reducedClassname="ring-2 ring-red-500"
>
  <slot />
</Highlighter>
