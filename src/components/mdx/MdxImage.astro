---
import { Image } from "astro:assets";
interface Props {
  src: ImageMetadata;
  alt: string;
}

const { src, alt } = Astro.props;
---

<Image src={src} alt={alt} widths={[500, 850]} sizes="(max-width: 1024px) 500px, 850px" />
