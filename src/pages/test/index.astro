---
import { getCollection } from "astro:content";

if (import.meta.env.PROD) return Astro.redirect("/");

const postName = "astro-blog-from-scratch";
const post = await getCollection("posts", ({ slug }) => slug === postName).then((r) => r[0]);
---
